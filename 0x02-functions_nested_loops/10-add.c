/**
 * add - receives two integer and add them
 * @x: receives first integer
 * @y: receives second integer
 *
 * Return: the addition of the two integers
 */
int add(int x, int y)
{
	return (x + y);
}
