/**
 * mul - multiplies two numbers
 * @a: receives the first number for multiplication
 * @b: receives the second number for multiplication
 *
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
